# Lean requirements for Streamlit Community Cloud deployment.
# The dashboard is self-contained (hardcoded illustrative data) and never
# calls the LLM or loads PyTorch, keeping the Cloud build small.

streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.0,<3.0
numpy>=1.26,<2.0
qrcode>=7.4.2
Pillow>=10.0.0
