# Abstract

Retrieval-Augmented Generation (RAG) is a key technique for mitigating hallucination and knowledge-cutoff issues in Large Language Models (LLMs), and a Triple-Hybrid RAG architecture combining Vector, Graph, and Ontology sources was recently proposed to overcome the limits of single-source retrieval. The author's prior work, Shin & Moon (2025, JKSCI), introduced the Rule-based Dynamic Weighting Algorithm (R-DWA) that determines the three source weights (α, β, γ) via a two-stage rule, but the rule-based nature imposes structural limits (domain bias, single-parameter λ, lack of conditional-query resolution, no cross-domain transfer).

This dissertation addresses these limits via **three-layer contributions**.

**(1) Reproducibility.** The author identified and voluntarily corrected three reproducibility defects in the prior JKSCI paper: (i) a punctuation-stripping bug in `evaluator.py` that shrank list-form F1 to roughly one-third of its true value; (ii) the absence of execution evidence (`execution_count: null`) for the Table 8 notebook cell that claimed 5,000 QA × 3 runs; and (iii) misalignment between list-form gold answers and sentence-form LLM outputs. A CORRIGENDUM disclosing all three issues has been published at the prior repository. These corrections establish a **trustworthy new baseline** (corrected R-DWA, F1<sub>strict</sub> = 0.529) against which the core contributions of this work are measured.

**(2) Methodological.** This work formalizes the weight-decision problem as a one-step Markov Decision Process (18-dimensional state × 3-simplex action) and proposes **Learned Dynamic Weighting Algorithm (L-DWA)**, which trains an Actor-Critic network (5,636 parameters) via Proximal Policy Optimization (PPO). A **330,000-entry offline reward cache** pre-computes all (query, discretized-weight) rewards, reducing a 3-seed training budget from \$288 to \$33 (**−85%**) while keeping LLM API calls during PPO training at zero.

**(3) Empirical.** On the corrected baseline, L-DWA outperforms R-DWA by **F1<sub>strict</sub> +6.2%** (0.529→**0.562 ± 0.007**, 3-seed std < 1.2%), **F1<sub>substring</sub> +5.2%** (0.482→**0.507**), and **Faithfulness +6.6%** (0.544→**0.580**). Most importantly, L-DWA's continuous Dirichlet mean **exceeds** the per-query argmax Oracle (defined over a 66-point discrete grid) on *every* F1 metric (F1<sub>strict</sub>: 0.562 > 0.554; F1<sub>sub</sub>: 0.507 > 0.504; F1<sub>char</sub>: 0.494 > 0.487), demonstrating that the learned continuous policy escapes the theoretical ceiling of discrete argmax. The conditional-query subtype shows a qualitative gap of **+36.7%** over R-DWA (0.223→0.304), likewise exceeding Oracle (0.290).

**Cross-domain analysis.** Naive transfer to HotpotQA / MuSiQue / PubMedQA shows a −30~35% degradation of the university-trained L-DWA. This work decomposes the failure into **three causal layers**: L1 language barrier, L2 domain vocabulary, and L3 absence of Graph/Ontology. Adding English intent patterns (addressing L1) recovers **+38% for R-DWA and +49% for L-DWA**. A newly constructed **English synthetic university benchmark** (403 QA) shows R-DWA reaching F1<sub>strict</sub> = **0.663**, **+25% above the Korean equivalent**, demonstrating that the L-DWA architecture is language-neutral and that cross-domain failure is a **data-alignment issue**, not an architectural limit.

All code, the 330K-entry reward cache, 3-seed checkpoints, and a Streamlit dashboard are publicly available at github.com/sdw1621/triple-rag-phd for one-command reproduction.

**Keywords**: Triple-Hybrid RAG, Reinforcement Learning, Proximal Policy Optimization, Learned Dynamic Weighting, Offline Reward Cache, Korean QA, Knowledge Graph, Ontology, Reproducibility, Cross-lingual Transfer
