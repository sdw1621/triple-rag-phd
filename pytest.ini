[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests requiring network or external services (skipped in unit runs)
    slow: tests that take >5s
addopts = -ra --strict-markers
